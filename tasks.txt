# 🧃 Apple Juice — AI Generation Improvement Tasks

Ordered by recommended sequence (visible impact first, then the deep agentic upgrade).
[x] = done, [ ] = pending. One task at a time; tick + report before moving on.

## ── HIGH IMPACT ──

[x] 5. UI library context for the agent — app passes uiContext (libraryDeploymentPrompt + uiExamplesBlock) to /v1/agent when isUIRelatedPrompt; proxy injects it into the agent instructions under a "UI LIBRARY" section. Build tag -> v7-uicontext.

[x] 1. Agentic playtest feedback loop — the existing auto-fix loop now closes for the agent path: agent writes -> syncs -> playtest -> [APPLE_JUICE_TEST_FAIL] -> auto-fix turn re-runs the agent. Made the agent always pull a FRESH snapshot each run (so the fix turn sees the just-synced broken code), and rewrote buildAutoFixPrompt to tell the filesystem agent to read the live files first. Bounded to MAX_AUTO_FIX retries.

[x] 3. Validate generated Luau — validateGeneration already runs on the agent path (client finalization). Added a dependency-free sanityCheckLuau() in the proxy (balanced brackets/strings, block-end heuristic) that returns non-blocking warnings in the result; logged server-side. Tested 5/5 (no false positives on strings). Build tag -> v8-validate. (Full luau-analyze can be added later for deep checks.)

[ ] 2. On-demand live source reads
    When the snapshot truncated a file (400KB/8MB caps), let the agent request the
    full source of a specific script mid-run via the existing request-file flow,
    for accurate edits on large projects.

## ── MEDIUM IMPACT ──

[ ] 4. Persist session dir across prompts in a chat
    Keep the materialized dir warm between prompts (re-sync only changed files)
    so follow-up prompts ("now add X to that") are faster and more coherent.

[ ] 6. Model routing by task complexity
    Route trivial edits (rename, tweak) to a cheap/fast model; reserve Opus/Sonnet
    for architecture. Saves credits, faster, no quality loss on simple tasks.

## ── POLISH ──

[ ] 7. Trim final chat message
    Agent narration is already streamed as progress; the final chat bubble should
    show only the agent's summary, not the "Reading file..." chrome.

[ ] 8. Diff-aware / targeted editing
    Encourage targeted edits over full-file rewrites for small changes to large
    files (more reliable, less token waste).

[ ] 9. Instance-aware revert
    Extend diffToRevert to inverse the manifest diff too, so reverting also undoes
    instance creates (RemoteEvents, GUIs), not just scripts.
